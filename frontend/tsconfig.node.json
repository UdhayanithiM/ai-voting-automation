{
  // "$schema": "https://json.schemastore.org/tsconfig", // Uncomment if schema validation is needed and connection is stable
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "module": "CommonJS",
    "target": "ES2020",
    "noEmit": false,
    "types": ["node", "vite/client"],
    "rootDir": "."
  },
  "include": ["vite.config.ts", "**/*.ts"],
  "exclude": ["node_modules"]
  
}